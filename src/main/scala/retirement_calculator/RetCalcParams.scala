package retirement_calculator

case class RetCalcParams(
                        income: Double,
                        expenses: Double,
                        nMonthsInRetirement: Int,
                        initialCapital: Double
                        )
